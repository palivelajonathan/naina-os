[package]
name = "configuration"
version = "0.1.0"
edition = "2024"
description = "Configuration loading, validation, and management for NAINA OS"
readme = "README.md"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
thiserror = "2.0"

[dev-dependencies]
serial_test = "0.5"
tempfile = "3.10"
