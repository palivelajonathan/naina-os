[package]
name = "sdk"
version = "0.1.0"
edition = "2021"
description = "NAINA OS primary client SDK facade package"

[dependencies]
runtime = { path = "../runtime" }
services = { path = "../services" }
configuration = { path = "../configuration" }
logging = { path = "../logging" }

[dev-dependencies]
kernel = { path = "../kernel" }
