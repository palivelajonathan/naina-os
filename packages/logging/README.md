# NAINA OS Logging Package

The `logging` package provides NAINA OS logging core types and interfaces.

This package is intentionally small in Sprint 0: it starts with the `LogLevel`
contract and expands later to include logger, records, sinks, and filters.
