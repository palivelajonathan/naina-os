[package]
name = "memory"
version = "0.1.0"
edition = "2024"
description = "Persistent memory storage, document indexing, and hybrid retrieval for NAINA OS"
readme = "README.md"

[dependencies]
configuration = { path = "../configuration" }
logging = { path = "../logging" }
