[package]
name = "browser-runtime"
version = "0.1.0"
edition = "2021"
description = "Browser automation runtime manager (Chromium CDP) for NAINA OS"

[dependencies]
runtime = { path = "../runtime" }
services = { path = "../services" }
configuration = { path = "../configuration" }
logging = { path = "../logging" }

[dev-dependencies]
kernel = { path = "../kernel" }
